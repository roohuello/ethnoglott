# Runtime (server-only, never NEXT_PUBLIC — lib/data/groups.ts is server-only)
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Direct DB access (psql "$DIRECT_URL" -f supabase/seed/....sql for seed writes).
# Never expose to prod runtime; no owner-role client exists in app/.
DIRECT_URL=
DATABASE_URL=
